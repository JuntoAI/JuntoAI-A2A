[pytest]
asyncio_mode = auto
addopts = --cov=app --cov-fail-under=70 --hypothesis-seed=0
testpaths = tests
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (FastAPI TestClient)
    property: Property-based tests (Hypothesis)
    slow: Slow tests (>5s)
