FROM node:20-slim

WORKDIR /opt/test-deps

COPY package.json package-lock.json ./
RUN npm ci --silent

ENV NODE_PATH=/opt/test-deps/node_modules
ENV PATH="/opt/test-deps/node_modules/.bin:$PATH"

WORKDIR /workspace/frontend
